# Vercel Postgres connection string.
# In production this is injected automatically once you connect a Postgres
# database to this project in the Vercel dashboard (Storage tab).
# For local development, run `vercel env pull .env.local` after linking the
# project, or paste the connection string from your database provider here.
POSTGRES_URL="postgres://user:password@host:5432/dbname"
